##Total sessions and bounce rate by traffic source

SELECT
  source,
  COUNT(DISTINCT session_id) AS total_sessions,
  ROUND(SUM(CASE WHEN bounce = 1 THEN 1 ELSE 0 END) * 100.0 / COUNT(*), 2) AS bounce_rate
FROM
  web_sessions
GROUP BY source
ORDER BY total_sessions DESC;
